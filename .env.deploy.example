# Example environment file for docker-compose deployment (DO NOT COMMIT REAL SECRETS)
# Copy this to .env.deploy and fill in real values on the VPS before running docker compose.

# MongoDB connection string (Atlas or your VPS-hosted MongoDB)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/fastingDB

# JWT secret (use a long random value)
JWT_SECRET=replace_with_a_long_random_string

# Email credentials used by server (if you use Nodemailer)
EMAIL_USER=you@example.com
EMAIL_PASS=supersecretpassword

# Optionally override server port (default 8080 in Dockerfile.server)
PORT=8080
